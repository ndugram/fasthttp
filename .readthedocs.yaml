version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3"
  commands:
    - pip install mkdocs==1.6.1 mkdocs-material==9.5.18
    - mkdocs build -d $READTHEDOCS_OUTPUT/html

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false