version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      packages:
        - mkdocs
        - mkdocs-material

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false
