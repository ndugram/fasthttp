version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - python -m pip install --upgrade pip
    - python -m pip install mkdocs==1.6.1 mkdocs-material==9.5.18
    - python -m mkdocs build -d $READTHEDOCS_OUTPUT/html

mkdocs:
  configuration: mkdocs.yml